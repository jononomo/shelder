import os, subprocess

def after_install(options, home_dir):
    print 'after install here!!!'
    # jonti_home = os.environ['JONTI_HOME']
    # call_subprocess(['source', jonti_home+'/bin/JONTI/bin/activate'], show_stdout=True)
    # call_subprocess(['pip', 'install' '-r', jonti_home+'/bin/requirements.txt'], show_stdout=True)

